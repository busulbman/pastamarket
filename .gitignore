# bağımlılıklar
/node_modules

# Next.js build çıktıları ve cache
/.next
/out
/.next-build-cache*
next-env.d.ts
*.tsbuildinfo

# ortam değişkenleri
.env
.env.local
.env.*.local

# veritabanı (yerel geliştirme)
/data/*.db
/data/*.db-shm
/data/*.db-wal

# yönetim panelinden yüklenen görseller
/public/uploads/*
!/public/uploads/.gitkeep

# işletim sistemi
.DS_Store
*.log

# yerel veritabanı yedekleri (deploy edilmez)
/data/backups/
