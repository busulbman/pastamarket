import Link from "next/link";
import { categories, products } from "@/lib/data";
import { ProductActions } from "@/components/panel/product-manager";
export const dynamic = "force-dynamic";
export default async function PanelProducts() { const [list, categoryList] = await Promise.all([products({ includeInactive: true }), categories(false)]); return <><div className="flex flex-wrap items-center justify-between gap-3"><div><h1 className="text-2xl font-extrabold text-ink">Ürünler</h1><p className="mt-1 text-sm text-muted">{list.length} ürün</p></div><Link href="/panel/urunler/yeni" className="rounded-lg bg-brand px-4 py-2.5 text-sm font-bold text-white">Yeni ürün</Link></div><div className="card mt-6 overflow-x-auto"><table className="w-full min-w-[680px] text-left text-sm"><thead className="border-b border-line-soft bg-zinc-50 text-muted"><tr><th className="p-3">Ürün</th><th className="p-3">Kategori</th><th className="p-3">Fiyat</th><th className="p-3">Durum</th><th className="p-3"></th></tr></thead><tbody>{list.map((product) => <tr key={product.id} className="border-b border-line-soft"><td className="p-3 font-semibold text-ink">{product.name}<small className="block font-normal text-muted">{product.brand} · {product.weight}</small></td><td className="p-3">{product.categoryName || categoryList.find((item) => item.id === product.categoryId)?.name}</td><td className="p-3">₺{(product.salePrice ?? product.price).toFixed(2)}</td><td className="p-3">{product.active ? "Aktif" : "Pasif"}</td><td className="p-3"><ProductActions id={product.id} active={product.active}/></td></tr>)}</tbody></table></div></>; }
