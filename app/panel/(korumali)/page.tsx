import Link from "next/link";
import { dashboardStats, recentOrders } from "@/lib/data";
export const dynamic = "force-dynamic";
export default async function PanelHome() { const [stats, orders] = await Promise.all([dashboardStats(), recentOrders()]); return <><h1 className="text-2xl font-extrabold text-ink">Kontrol paneli</h1><div className="mt-6 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{[["Toplam ürün", stats.totalProducts], ["Aktif ürün", stats.activeProducts], ["Kategori", stats.categories], ["Sipariş", stats.totalOrders]].map(([label, value]) => <div key={String(label)} className="card p-5"><p className="text-sm text-muted">{label}</p><b className="mt-1 block text-3xl text-ink">{value}</b></div>)}</div><section className="mt-8"><div className="flex items-center justify-between"><h2 className="text-lg font-bold">Son siparişler</h2><Link href="/panel/siparisler" className="text-sm font-bold text-brand">Tümünü gör</Link></div>{orders.length ? <div className="card mt-3 overflow-x-auto"><table className="w-full text-left text-sm"><thead><tr className="border-b border-line-soft text-muted"><th className="p-3">No</th><th className="p-3">Müşteri</th><th className="p-3">Tutar</th><th className="p-3">Durum</th></tr></thead><tbody>{orders.map((order) => <tr key={order.id}><td className="p-3">{order.order_number}</td><td className="p-3">{order.first_name} {order.last_name}</td><td className="p-3">₺{order.total.toFixed(2)}</td><td className="p-3">{order.status}</td></tr>)}</tbody></table></div> : <p className="mt-3 text-sm text-muted">Henüz sipariş yok.</p>}</section></>; }
